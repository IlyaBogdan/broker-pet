/**
 * Set user online
 * not implemented
 * 
 * TODO: later will be shared method
 */
export const setOnline = (user: Object) => {
    //user.active = true;
    //broadCast.emit('broadcast', { method: 'setUserList', users: broker.users });
};
